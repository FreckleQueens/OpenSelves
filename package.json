{
	"name": "openselves",
	"version": "0.6.2",
	"scripts": {
		"dev": "concurrently --kill-others \"cd common && yarn dev\" \"cd common-ui && yarn dev\" \"cd client && yarn dev\" \"cd server && yarn dev\" \"cd www && yarn dev\"",
		"clean": "yarn workspaces foreach -Ap run clean",
		"build": "yarn clean && yarn workspaces foreach -Rpt --from \"{client,server,www}\" run build",
		"lint": "yarn build && yarn workspaces foreach -Ap run lint",
		"prepare": "husky"
	},
	"packageManager": "yarn@4.13.0",
	"devDependencies": {
		"concurrently": "^10.0.3",
		"husky": "^9.1.7"
	},
	"workspaces": [
		"common",
		"common-ui",
		"client",
		"server",
		"www"
	]
}
