{
	"name": "openselves",
	"version": "0.1.0",
	"scripts": {
		"dev": "concurrently --kill-others \"cd client && yarn dev\" \"cd server && yarn dev\"",
		"ci:build": "cd common && yarn build && cd ../client && yarn build && cd ../server && yarn build",
		"ci:test": "cd server && yarn test:e2e"
	},
	"packageManager": "yarn@4.13.0",
	"devDependencies": {
		"concurrently": "^9.2.1"
	},
	"workspaces": [
		"common",
		"client",
		"server"
	]
}
