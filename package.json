{
	"name": "openselves",
	"version": "0.5.0",
	"scripts": {
		"dev": "concurrently --kill-others \"cd client && yarn dev\" \"cd server && yarn dev\"",
		"ci:build": "yarn workspaces foreach --all run prepare && cd common && yarn build && cd ../client && yarn build && cd ../server && yarn build",
		"prepare": "husky"
	},
	"packageManager": "yarn@4.13.0",
	"devDependencies": {
		"concurrently": "^9.2.1",
		"husky": "^9.1.7"
	},
	"workspaces": [
		"common",
		"client",
		"server"
	]
}
